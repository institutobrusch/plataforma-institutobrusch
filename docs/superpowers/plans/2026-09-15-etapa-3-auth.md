# Etapa 3 — Autenticação real + área logada no Supabase — Plano

> **For agentic workers:** implementar tarefa a tarefa; cada tarefa termina com build/verificação. Nada é publicado/deployado (isso é a Etapa 6). Trabalho local; push ao GitHub institutobrusch ao final.

**Goal:** Substituir a autenticação simulada por **Supabase Auth real** (e-mail/senha + Google), com cadastro **apenas por compra/convite**, e ligar a área logada (`/app`) aos dados reais do banco criado na Etapa 2, respeitando o RLS.

**Architecture:** `@supabase/ssr` com refresh de sessão no `proxy.ts` (ex-middleware do Next 16) + clients server/browser já criados na Etapa 2. Autorização de rota feita no **servidor** (layout de `/app`), não só no proxy. Cadastro fechado: contas nascem via `admin.createUser` (service role) no resgate de convite; o toggle de signup público fica **desligado**.

**Tech Stack:** Next 16 (App Router, Server Actions, `proxy.ts`), `@supabase/ssr` 0.12, Supabase Auth (email + Google OAuth), Postgres/RLS da Etapa 2.

**Projeto Supabase:** `jnwjrjzinqqcxlvleznq` (institutobrusch).

---

## Pré-requisitos (ações do Iago no painel Supabase — eu não faço por segurança)
1. **Service role key** → Project Settings → API → copiar `service_role` e adicionar em `platform/.env.local` como `SUPABASE_SERVICE_ROLE_KEY=...` (secret, **nunca** com prefixo `NEXT_PUBLIC`). Também irá nas env vars da Vercel na Etapa 6.
2. **Fechar cadastro público** → Authentication → Sign In / Providers → **desabilitar** "Allow new users to sign up" (contas só via convite/admin).
3. **Google provider** → Authentication → Providers → Google → habilitar e colar Client ID/Secret de um OAuth client do Google Cloud. Redirect URL a cadastrar no Google: `https://jnwjrjzinqqcxlvleznq.supabase.co/auth/v1/callback`. (Pode ficar para depois; o login e-mail/senha já funciona sem isso.)
4. **URLs de redirecionamento** → Authentication → URL Configuration → adicionar `http://localhost:3000/**` (dev) e depois o domínio de produção. Site URL = `http://localhost:3000` por enquanto.

---

## Task 1 — Refresh de sessão (proxy.ts) + helper de middleware
**Files:** Create `src/lib/supabase/middleware.ts`, `src/proxy.ts`

- [ ] **Step 1:** `src/lib/supabase/middleware.ts`
```ts
import { createServerClient } from "@supabase/ssr";
import { NextResponse, type NextRequest } from "next/server";
import type { Database } from "@/lib/database.types";

export async function updateSession(request: NextRequest) {
  let response = NextResponse.next({ request });
  const supabase = createServerClient<Database>(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() { return request.cookies.getAll(); },
        setAll(cookiesToSet) {
          cookiesToSet.forEach(({ name, value }) => request.cookies.set(name, value));
          response = NextResponse.next({ request });
          cookiesToSet.forEach(({ name, value, options }) => response.cookies.set(name, value, options));
        },
      },
    },
  );
  // Importante: renova o token. Não fazer autorização aqui (só refresh).
  await supabase.auth.getUser();
  return response;
}
```
- [ ] **Step 2:** `src/proxy.ts`
```ts
import type { NextRequest } from "next/server";
import { updateSession } from "@/lib/supabase/middleware";

export async function proxy(request: NextRequest) {
  return await updateSession(request);
}

export const config = {
  matcher: ["/((?!_next/static|_next/image|favicon.ico|brand|fotos|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)"],
};
```
- [ ] **Step 3:** `npm run build` deve compilar.

## Task 2 — Helpers de autenticação (servidor)
**Files:** Create `src/lib/auth.ts`
- [ ] **Step 1:** funções que centralizam sessão, perfil e entitlements:
```ts
import { redirect } from "next/navigation";
import { createClient } from "@/lib/supabase/server";

export async function getUser() {
  const supabase = await createClient();
  const { data: { user } } = await supabase.auth.getUser();
  return user;
}
export async function getProfile() {
  const supabase = await createClient();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return null;
  const { data } = await supabase.from("profiles").select("*").eq("id", user.id).single();
  return data;
}
// slugs dos produtos ativos do usuário (para montar o menu lateral)
export async function getMeusProdutos(): Promise<string[]> {
  const supabase = await createClient();
  const { data } = await supabase
    .from("user_products")
    .select("status, products(slug, tipo, nome)")
    .eq("status", "ativo");
  return (data ?? []).map((r) => (r.products as { slug: string }).slug);
}
export async function requireUser() {
  const user = await getUser();
  if (!user) redirect("/entrar");
  return user;
}
```

## Task 3 — Login real + OAuth callback + logout
**Files:** Rewrite `src/app/entrar/page.tsx` (form → Server Actions), Create `src/app/auth/actions.ts`, `src/app/auth/callback/route.ts`, `src/app/auth/signout/route.ts`
- [ ] **Step 1:** `src/app/auth/actions.ts` (`"use server"`):
```ts
"use server";
import { redirect } from "next/navigation";
import { createClient } from "@/lib/supabase/server";

export async function entrarComEmail(formData: FormData) {
  const email = String(formData.get("email"));
  const password = String(formData.get("senha"));
  const supabase = await createClient();
  const { error } = await supabase.auth.signInWithPassword({ email, password });
  if (error) redirect("/entrar?erro=credenciais");
  redirect("/app");
}
export async function entrarComGoogle() {
  const supabase = await createClient();
  const { data } = await supabase.auth.signInWithOAuth({
    provider: "google",
    options: { redirectTo: `${process.env.NEXT_PUBLIC_SITE_URL ?? "http://localhost:3000"}/auth/callback` },
  });
  if (data.url) redirect(data.url);
}
```
- [ ] **Step 2:** `src/app/auth/callback/route.ts` troca o code por sessão:
```ts
import { NextResponse } from "next/server";
import { createClient } from "@/lib/supabase/server";
export async function GET(request: Request) {
  const { searchParams, origin } = new URL(request.url);
  const code = searchParams.get("code");
  if (code) {
    const supabase = await createClient();
    await supabase.auth.exchangeCodeForSession(code);
  }
  return NextResponse.redirect(`${origin}/app`);
}
```
- [ ] **Step 3:** `src/app/auth/signout/route.ts` (POST) chama `supabase.auth.signOut()` e redireciona para `/`.
- [ ] **Step 4:** Reescrever `entrar/page.tsx`: form e-mail/senha usa `action={entrarComEmail}`; botão Google usa `action={entrarComGoogle}`. Remover o "Entrar como cliente demo" e o `demoLogin`. Mostrar erro se `?erro=credenciais`. Adicionar link "Recebeu um convite?" → `/convite`.
- [ ] **Step 5:** Build.

## Task 4 — Proteger /app e montar o menu pelos produtos reais
**Files:** Edit `src/app/app/layout.tsx`, rewrite `src/components/AppShell.tsx` (deixa de ser guarda client; vira casca visual), Delete `src/lib/demoAuth.ts`
- [ ] **Step 1:** `app/layout.tsx` (server): `const user = await requireUser();` + `const produtos = await getMeusProdutos();` + `const profile = await getProfile();` e passa `nome`, `email`, `produtos` para `<AppShell>`.
- [ ] **Step 2:** `AppShell` vira componente que recebe props (nome, email, produtos[], children) e ainda é client só para o estado do drawer e o link ativo. Menu "Meus produtos" renderiza dinamicamente: se `produtos.includes("cartografia")` mostra Cartografia; idem para cursos/ebooks futuramente. Logout vira `<form action="/auth/signout" method="post">`.
- [ ] **Step 3:** Remover `demoAuth` e qualquer `isDemoLogged`. Build.

## Task 5 — Pensamento diário do banco
**Files:** Rewrite `src/app/app/page.tsx` (server)
- [ ] **Step 1:** `const supabase = await createClient(); const { data } = await supabase.from("daily_thoughts").select("*").order("data",{ascending:false});` Render: o primeiro como "hoje", resto em "anteriores". Áudio: se `audio_path`, gerar signed URL (bucket `audios`) via `supabase.storage.from("audios").createSignedUrl(path, 3600)`. Player recebe a URL.
- [ ] **Step 2:** Estado vazio tratado ("Ainda não há pensamentos publicados."). Build.

## Task 6 — Comunidade real (moderação prévia)
**Files:** rewrite `src/app/app/comunidade/page.tsx` (server carrega, client interage), Create `src/app/app/comunidade/actions.ts`
- [ ] **Step 1:** Server carrega posts aprovados + do próprio autor (RLS já filtra): `select *, profiles(nome), community_likes(count), community_comments(count)` ordenado por `created_at desc`. Marca se o usuário atual curtiu.
- [ ] **Step 2:** `actions.ts` (`"use server"`): `publicarPost(texto)` insere em `community_posts` com `author_id = auth.uid()` e status default `pendente` (RLS exige); `curtir(postId)` faz upsert/delete em `community_likes`. `revalidatePath("/app/comunidade")`.
- [ ] **Step 3:** UI: caixa de publicação chama a action e mostra "aguardando aprovação"; feed lista aprovados; curtir chama action. Comentários: exibir contagem (criação de comentário pode ficar para um incremento posterior). Build.

## Task 7 — Cartografia real (por usuário, gated)
**Files:** rewrite `src/app/app/cartografia/page.tsx` (server)
- [ ] **Step 1:** Checar acesso: se `!produtos.includes("cartografia")` → mostrar tela "Você ainda não tem a Cartografia" + CTA. Senão carregar `carto_cartas` (1), `carto_sessions` (ordem), `carto_materials` do usuário (RLS já restringe ao dono + entitlement).
- [ ] **Step 2:** Áudios das sessões e materiais via **signed URLs** dos buckets `audios`/`materiais`. As abas (Minha carta/Sessões/Materiais) permanecem client, recebendo os dados por props.
- [ ] **Step 3:** Build.

## Task 8 — Resgate de convite (cadastro fechado)
**Files:** Create `src/lib/supabase/admin.ts` (service role, server-only), `src/app/convite/page.tsx` (informar token), `src/app/convite/[token]/page.tsx` + `actions.ts`
- [ ] **Step 1:** `admin.ts`:
```ts
import { createClient } from "@supabase/supabase-js";
import type { Database } from "@/lib/database.types";
// Client com service role — NUNCA importar em componente client.
export function createAdminClient() {
  return createClient<Database>(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!,
    { auth: { autoRefreshToken: false, persistSession: false } },
  );
}
```
- [ ] **Step 2:** `convite/[token]/actions.ts` (`"use server"`) `resgatar(token, nome, senha)`:
  1. valida convite (`invites` where token, status pendente, não expirado) via admin client;
  2. `admin.auth.admin.createUser({ email, password, email_confirm: true, user_metadata:{ name } })`;
  3. se o convite tem `product_id`, insere `user_products (user_id, product_id, origem:'convite')`;
  4. marca convite `usado`;
  5. faz login (`signInWithPassword`) e redireciona para `/app`.
- [ ] **Step 3:** Páginas de convite (form nome + senha). Tratar token inválido/expirado com mensagem clara.
- [ ] **Step 4:** (Admin gera convites — na Etapa 5/backoffice. Por ora, convites podem ser inseridos manualmente por SQL para teste.) Build.

## Task 9 — Sugestões para o banco
**Files:** rewrite `src/components/FormSugestao.tsx` + Create `src/app/(site)/contato/actions.ts`
- [ ] **Step 1:** action `enviarSugestao(texto)` insere em `suggestions` (RLS permite `user_id = auth.uid()` ou nulo p/ anônimo). Mantém validação e estado de sucesso. Build.

## Task 10 — Seed de contas (demo + admin) e dados de exemplo
**Files:** (execução via Supabase — dashboard + SQL)
- [ ] **Step 1:** No painel Authentication → Add user: criar a conta da **Camila** (admin) e uma conta **demo** (para reunião), ambas com senha.
- [ ] **Step 2:** SQL (via MCP) para: setar `profiles.papel='admin'` da Camila; conceder `user_products` de `cartografia` ao demo; inserir 1 `carto_cartas`, 3 `carto_sessions` e alguns `carto_materials` para o demo; publicar 2–3 `daily_thoughts`. (Áudios reais podem ser enviados depois; por ora sem `audio_path` ou placeholder.)
- [ ] **Step 3:** Aprovar 2–3 `community_posts` de exemplo (status `aprovado`).

## Task 11 — Limpeza + verificação
- [ ] **Step 1:** Remover restos da demo (localStorage), garantir que `/app/*` redireciona para `/entrar` sem sessão (testar deslogado).
- [ ] **Step 2:** Testes: manter os de conteúdo; adicionar teste util de `getMeusProdutos` mapping se fizer sentido (mock). `npm run build` limpo; `npm test` verde.
- [ ] **Step 3:** `get_advisors` (security) após as mudanças; corrigir o que aparecer.
- [ ] **Step 4:** Commit + push para `origin`.

---

## Verificação de aceite (fim da Etapa 3)
- Deslogado, `/app` → redireciona para `/entrar`.
- Login e-mail/senha entra; logout volta ao site.
- (Se Google configurado) login Google funciona via `/auth/callback`.
- Menu lateral mostra só os produtos que a conta tem.
- Pensamento diário, Comunidade (com moderação) e Cartografia leem do Supabase, com isolamento por usuário (um usuário não vê a carta de outro).
- Convite: token válido cria conta + concede produto; token inválido é barrado.

## Fora de escopo (próximas etapas)
- **Etapa 4:** vendas (checkout Asaas + webhook → entitlement), leitura de e-books na plataforma, player de cursos (YouTube gated), inscrições de eventos gravando no banco.
- **Etapa 5:** backoffice da Camila (publicar pensamento, gerenciar cartas/sessões/materiais por cliente, moderar comunidade, gerar convites, CRUD de catálogo).
- **Etapa 6:** deploy Vercel + domínio + e-mail transacional.
